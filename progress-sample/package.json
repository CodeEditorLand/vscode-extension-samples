{
	"activationEvents": [
		"*"
	],
	"contributes": {
		"commands": [
			{
				"command": "extension.startTask",
				"title": "Show Progress"
			}
		]
	},
	"description": "Show a long running operation in the notification area",
	"devDependencies": {
		"@types/node": "22"
	}
}
